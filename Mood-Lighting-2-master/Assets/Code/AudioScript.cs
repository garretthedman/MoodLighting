using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AudioScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

/* AUDIO COMPONENTS
 *
 * Background music for main menu page (~10 second loop) GOT
 * Transition for pressing buttons on main menu (~.5 sec) sort of got
 *
 * Audio feedback when selection is made (~.5 sec) GOT
 * Dramatic countdown/beeping audio (x10) sort of got
 * "Start game" audio clip when countdown ends (~ 1 sec) need
 * 
 * Wrong guess audio feedback (~.5, buzzer) GOT
 * Correct guess audio feedback (Ding!) GOT
 * Time run out sound (another buzzer) sort of got
 * 
 *
 * End of round cooldown music (waterfall/cascade) (~2 sec?) GOT
 *
 */




